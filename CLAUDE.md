# CLAUDE.md

프로젝트 공통 에이전트 가이드는 AGENTS.md 한 곳에서 관리한다(Codex와 공유). 아래 import로 전체 내용이 로드된다.

@AGENTS.md

## Claude Code 전용 참고

- 구조 질문은 `docs/folder-structure.md`를 먼저 읽는다 (루트가 아니라 `docs/` 아래에 있음).
- 이 파일에는 Claude Code에만 해당하는 내용만 추가한다. 프로젝트 규칙·구조·절차 변경은 `AGENTS.md`를 수정할 것.
